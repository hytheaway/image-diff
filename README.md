# image-diff
requires [PIL](https://pypi.org/project/pillow/): `pip install Pillow`

usage: `python3 image_diff.py path_to_image_1 path_to_image_2 path_to_output`

must include file extensions (including in the output!)
